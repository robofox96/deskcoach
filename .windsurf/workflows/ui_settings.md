---
description: Minimal settings & today’s stats page (Streamlit).
auto_execution_mode: 1
---

Command name: /ui_settings
Goal: Minimal settings & today’s stats page (Streamlit).

Inputs: rules; configuration schema; logs.

Steps:

Controls: start/stop, camera selector, thresholds, sustain windows, cooldowns.

Status: current state, metrics preview, confidence.

Today’s stats: time in good, nudges shown, accepted vs ignored.

Buttons: Recalibrate, Purge Data, Pause Monitoring.

Persist settings to config; reflect changes immediately.

Deliverables: single-page local UI wired to background service.
Out of scope: weekly charts (future milestone).